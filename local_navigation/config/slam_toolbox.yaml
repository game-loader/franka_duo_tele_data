slam_toolbox:
  ros__parameters:
    use_sim_time: false
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None

    odom_frame: odom
    map_frame: map
    base_frame: base_link
    scan_topic: /navigation/scan
    mode: mapping

    resolution: 0.05
    # The driver reports its 0.1 m limit as float32 0.100000001. Keep this
    # just above that value to avoid a misleading capability warning.
    min_laser_range: 0.101
    max_laser_range: 30.0
    minimum_time_interval: 0.10
    transform_publish_period: 0.05
    map_update_interval: 2.0
    minimum_travel_distance: 0.10
    minimum_travel_heading: 0.10
    scan_buffer_size: 10
    scan_buffer_maximum_scan_distance: 10.0
    use_scan_matching: true
    use_scan_barycenter: true
    do_loop_closing: true
    loop_search_maximum_distance: 3.0
